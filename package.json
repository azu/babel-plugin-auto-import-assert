{
  "name": "babel-plugin-auto-import-assert",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/azu/babel-plugin-auto-import-assert.git"
  },
  "author": "azu",
  "email": "azuciao@gmail.com",
  "homepage": "https://github.com/azu/babel-plugin-auto-import-assert",
  "license": "MIT",
  "files": [
    "src/",
    "lib/"
  ],
  "bugs": {
    "url": "https://github.com/azu/babel-plugin-auto-import-assert/issues"
  },
  "version": "1.0.2",
  "description": "babel plugin that auto import assert module.",
  "main": "lib/index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "NODE_ENV=production babel src --out-dir lib --source-maps",
    "watch": "babel src --out-dir lib --watch --source-maps",
    "prepublish": "npm run --if-present build",
    "test": "mocha"
  },
  "keywords": [
    "babel",
    "plugin",
    "assert"
  ],
  "devDependencies": {
    "babel-cli": "^6.7.5",
    "babel-core": "^6.7.6",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-power-assert": "^1.0.0",
    "babel-register": "^6.7.2",
    "mocha": "^2.4.5"
  },
  "dependencies": {
    "babel-template": "^6.7.0"
  }
}
